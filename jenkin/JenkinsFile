pipeline {
    agent any
    stages {
        stage("Clean Up") {
            steps {
                deleteDir()
            }
        }
        stage("Clone Repo") {
            steps {
                sh "git clone https://github.com/tmtam612/clothes.git"
            }
        }
        stage("Build") {
            steps {
                dir("clothes") {
                    sh "npm run install"
                }
            }
        }
        stage("Test") {
            steps {
                dir("clothes") {
                    sh "npm run test"
                }
            }
        }
    }
}